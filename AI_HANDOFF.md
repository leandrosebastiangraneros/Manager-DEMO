# 🚀 Inicio de Proyecto: NovaManager Commercial

**Este archivo contiene el contexto necesario para que tu Asistente de IA entienda el estado del proyecto inmediatamente.**

## 📜 Historia y Origen
Este proyecto es un **spin-off comercial** de `NovaManager`, originalmente parte de un portfolio personal. Fue extraído a esta carpeta independiente para desarrollarlo como un producto de software para la venta (SaaS/On-premise).

## 🎯 Objetivo Principal
Refactorizar y mejorar el código para que sea un producto robusto, seguro y configurable para múltiples clientes, eliminando la dependencia del entorno "Portfolio".

## 🛠️ Estado Técnico Actual
- **Frontend (React)**: Copiado desde `public/NovaManager/frontend`. Dependencias pendientes de instalar (`npm install`).
- **Backend (Python/FastAPI)**: Copiado desde `public/NovaManager/backend`. Dependencias pendientes (`pip install`).
- **Base de Datos**: Usa SQLite por defecto.
- **Git**: No inicializado (Fresh start).

## 📋 Lista de Tareas Inmediatas (To-Do)
1.  [ ] Inicializar repositorio Git (`git init`).
2.  [ ] Instalar dependencias en `frontend` y `backend`.
3.  [ ] Revisar `vite.config.js` y `config.js` para asegurar rutas absolutas/relativas correctas.
4.  [ ] Crear archivo `.env` para secretos (DB, API Keys).
5.  [ ] Renombrar/Rebranding (quitar referencias personales).
